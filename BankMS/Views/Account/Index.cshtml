@model IEnumerable<BankMS.Models.Account>




@{
    ViewData["Title"] = "Bank Account Management";
}

<div class="container-fluid">
    <!-- Header Section -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="fw-bold text-primary mb-1">Bank Account Management</h2>
            <p class="text-muted">View and manage all bank accounts in the system</p>
        </div>
        <div>
            <a asp-controller="Account"
               asp-action="CreateOrEdit"
               class="btn btn-primary">
                <i class="bi bi-plus-circle me-2"></i>
                Create New Account
            </a>
        </div>

    </div>

    <!-- Statistics Cards -->
    <div class="row mb-4">
        <div class="col-md-3 col-sm-6 mb-3">
            <div class="card border-start border-primary border-4 shadow-sm h-100">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h6 class="text-muted mb-1">Total Accounts</h6>
                            <h3 class="fw-bold mb-0">@Model.Count()</h3>
                        </div>
                        <div class="bg-primary bg-opacity-10 p-3 rounded-circle">
                            <i class="fas fa-wallet text-primary fa-lg"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
        <div class="col-md-3 col-sm-6 mb-3">
            <div class="card border-start border-success border-4 shadow-sm h-100">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h6 class="text-muted mb-1">Total Balance</h6>
                            <h3 class="fw-bold mb-0">@Model.Sum(a => a.Balance).ToString("C")</h3>
                        </div>
                        <div class="bg-success bg-opacity-10 p-3 rounded-circle">
                            <i class="fas fa-chart-line text-success fa-lg"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
        <div class="col-md-3 col-sm-6 mb-3">
            <div class="card border-start border-info border-4 shadow-sm h-100">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h6 class="text-muted mb-1">Active Accounts</h6>
                            <h3 class="fw-bold mb-0">@Model.Count(a => a.Status == "Active")</h3>
                        </div>
                        <div class="bg-info bg-opacity-10 p-3 rounded-circle">
                            <i class="fas fa-check-circle text-info fa-lg"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
        <div class="col-md-3 col-sm-6 mb-3">
            <div class="card border-start border-warning border-4 shadow-sm h-100">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h6 class="text-muted mb-1">Inactive Accounts</h6>
                            <h3 class="fw-bold mb-0">@Model.Count(a => a.Status == "Inactive")</h3>
                        </div>
                        <div class="bg-warning bg-opacity-10 p-3 rounded-circle">
                            <i class="fas fa-exclamation-circle text-warning fa-lg"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Filter and Search Section -->
    <div class="card shadow-sm mb-4">
        <div class="card-body">
            <div class="row g-3">
                <div class="col-md-4">
                    <div class="input-group">
                        <span class="input-group-text bg-light border-end-0">
                            <i class="fas fa-search text-muted"></i>
                        </span>
                        <input type="text" id="searchInput" class="form-control" placeholder="Search by account number or type...">
                    </div>
                </div>
                <div class="col-md-3">
                    <select id="statusFilter" class="form-select">
                        <option value="">All Status</option>
                        <option value="Active">Active</option>
                        <option value="Inactive">Inactive</option>
                        <option value="Pending">Pending</option>
                    </select>
                </div>
                <div class="col-md-3">
                    <select id="typeFilter" class="form-select">
                        <option value="">All Types</option>
                        <option value="Savings">Savings</option>
                        <option value="Checking">Checking</option>
                        <option value="Business">Business</option>
                    </select>
                </div>
                <div class="col-md-2">
                    <button id="resetFilters" class="btn btn-outline-secondary w-100">
                        <i class="fas fa-redo me-1"></i> Reset
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Accounts Table -->
    <div class="card shadow-sm">
        <div class="card-header bg-light py-3">
            <h5 class="mb-0 fw-bold">
                <i class="fas fa-table me-2"></i>All Bank Accounts
            </h5>
        </div>
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover mb-0" id="accountsTable">
                    <thead class="table-light">
                        <tr>
                            <th class="ps-4">
                                <div class="form-check">
                                    <input class="form-check-input" type="checkbox" id="selectAll">
                                </div>
                            </th>
                            <th>Account Number</th>
                            <th>Account Type</th>
                            <th>Balance</th>
                            <th>Status</th>
                            <th>Created Date</th>
                            <th class="text-end pe-4">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var account in Model)
                        {
                            <tr>
                                <td class="ps-4">
                                    <div class="form-check">
                                        <input class="form-check-input account-check" type="checkbox" value="@account.Id">
                                    </div>
                                </td>
                                <td>
                                    <div class="d-flex align-items-center">
                                        <div class="bg-primary bg-opacity-10 p-2 rounded me-3">
                                            <i class="fas fa-credit-card text-primary"></i>
                                        </div>
                                        <div>
                                            <strong>@account.AccountNumber</strong>
                                            <small class="d-block text-muted">ID: @account.Id</small>
                                        </div>
                                    </div>
                                </td>
                                <td>
                                    <span class="badge 
                                        @(account.AccountType == "Savings" ? "bg-success" : 
                                          account.AccountType == "Checking" ? "bg-info" : 
                                          "bg-primary") bg-opacity-10 text-@(account.AccountType == "Savings" ? "success" : 
                                          account.AccountType == "Checking" ? "info" : 
                                          "primary") px-3 py-2 rounded-pill">
                                        <i class="fas 
                                            @(account.AccountType == "Savings" ? "fa-piggy-bank" : 
                                              account.AccountType == "Checking" ? "fa-money-check" : 
                                              "fa-briefcase") me-2"></i>
                                        @account.AccountType
                                    </span>
                                </td>
                                <td>
                                    <div class="fw-bold @(account.Balance >= 0 ? "text-success" : "text-danger")">
                                        @account.Balance.ToString("C")
                                    </div>
                                    
                                </td>
                                <td>
                                    <span class="badge 
                                        @(account.Status == "Active" ? "bg-success" : 
                                          account.Status == "Inactive" ? "bg-secondary" : 
                                          "bg-warning") px-3 py-2">
                                        <i class="fas 
                                            @(account.Status == "Active" ? "fa-check-circle" : 
                                              account.Status == "Inactive" ? "fa-times-circle" : 
                                              "fa-clock") me-1"></i>
                                        @account.Status
                                    </span>
                                </td>
                                <td>
                                    <div>@account.CreatedDate.ToString("MMM dd, yyyy")</div>
                                    <small class="text-muted">@account.CreatedDate.ToString("hh:mm tt")</small>
                                </td>
                                <td class="text-end pe-4">
                                    <div class="btn-group" role="group">
                                        <a href="@Url.Action("Details", "Account", new { id = account.Id })" 
                                           class="btn btn-sm btn-outline-primary" 
                                           data-bs-toggle="tooltip" 
                                           title="View Details">
                                            <i class="fas fa-eye"></i>
                                        </a>
                                    <a href="@Url.Action("CreateOrEdit", "Account", new { id = account.Id })"
                                       class="btn btn-sm btn-outline-secondary"
                                       data-bs-toggle="tooltip"
                                       title="Edit Account">
                                        <i class="fas fa-edit"></i>
                                    </a>


                                        <form asp-action="Delete"
                                              asp-controller="Account"
                                              asp-route-id="@account.Id"
                                              method="post"
                                              class="d-inline"
                                              onsubmit="return confirm('Are you sure you want to delete this account?');">

                                            <button type="submit"
                                                    class="btn btn-sm btn-outline-danger"
                                                    data-bs-toggle="tooltip"
                                                    title="Delete Account">
                                                <i class="fas fa-trash"></i>
                                            </button>
                                        </form>

                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
        <div class="card-footer bg-light py-3">
            <div class="d-flex justify-content-between align-items-center">
                <div>
                    <span class="text-muted" id="selectedCount">0 accounts selected</span>
                </div>
                <div>
                    <button class="btn btn-outline-danger btn-sm me-2" id="bulkDeleteBtn" disabled>
                        <i class="fas fa-trash me-1"></i> Delete Selected
                    </button>
                    <button class="btn btn-outline-success btn-sm" id="exportBtn">
                        <i class="fas fa-download me-1"></i> Export to CSV
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Modal for Bulk Actions -->
<div class="modal fade" id="bulkActionModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Confirm Bulk Delete</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <p>Are you sure you want to delete <span id="selectedCountModal">0</span> selected accounts?</p>
                <div class="alert alert-warning">
                    <i class="fas fa-exclamation-triangle me-2"></i>
                    This action cannot be undone.
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-danger" id="confirmBulkDelete">Delete Accounts</button>
            </div>
        </div>
    </div>
</div>

<form method="post">
    @Html.AntiForgeryToken()
</form>




    @section Scripts {
<script>
document.addEventListener('DOMContentLoaded', function () {

    // Tooltips
    document.querySelectorAll('[data-bs-toggle="tooltip"]').forEach(el => {
        new bootstrap.Tooltip(el);
    });

    const searchInput = document.getElementById('searchInput');
    const statusFilter = document.getElementById('statusFilter');
    const typeFilter = document.getElementById('typeFilter');
    const rows = document.querySelectorAll('#accountsTable tbody tr');

    const selectAllCheckbox = document.getElementById('selectAll');
    const accountCheckboxes = document.querySelectorAll('.account-check');
    const selectedCount = document.getElementById('selectedCount');
    const bulkDeleteBtn = document.getElementById('bulkDeleteBtn');

    const bulkActionModal = new bootstrap.Modal(document.getElementById('bulkActionModal'));
    const selectedCountModal = document.getElementById('selectedCountModal');
    const confirmBulkDelete = document.getElementById('confirmBulkDelete');

    // Filter
    function filterTable() {
        const search = searchInput.value.toLowerCase();
        const status = statusFilter.value;
        const type = typeFilter.value;

        rows.forEach(row => {
            const accNo = row.cells[1].innerText.toLowerCase();
            const accType = row.cells[2].innerText;
            const accStatus = row.cells[4].innerText;

            const ok =
                (accNo.includes(search) || accType.toLowerCase().includes(search)) &&
                (!status || accStatus.includes(status)) &&
                (!type || accType.includes(type));

            row.style.display = ok ? '' : 'none';
        });
    }

    searchInput.addEventListener('input', filterTable);
    statusFilter.addEventListener('change', filterTable);
    typeFilter.addEventListener('change', filterTable);

    // Select all
    selectAllCheckbox.addEventListener('change', () => {
        accountCheckboxes.forEach(cb => cb.checked = selectAllCheckbox.checked);
        updateSelectedCount();
    });

    accountCheckboxes.forEach(cb => {
        cb.addEventListener('change', updateSelectedCount);
    });

    function updateSelectedCount() {
        const count = [...accountCheckboxes].filter(cb => cb.checked).length;
        selectedCount.innerText = `${count} accounts selected`;
        selectedCountModal.innerText = count;
        bulkDeleteBtn.disabled = count === 0;
    }

    bulkDeleteBtn.addEventListener('click', () => {
        bulkActionModal.show();
    });

    // ✅ REAL BULK DELETE
    confirmBulkDelete.addEventListener('click', function () {

        const ids = [...accountCheckboxes]
            .filter(cb => cb.checked)
            .map(cb => parseInt(cb.value));

        fetch('/Account/BulkDelete', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
                'RequestVerificationToken':
                    document.querySelector('input[name="__RequestVerificationToken"]').value
            },
            body: JSON.stringify(ids)
        })
        .then(res => res.ok ? location.reload() : alert('Delete failed'));
    });

    updateSelectedCount();
});
</script>




 @*    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Initialize tooltips
            var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'))
            var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
                return new bootstrap.Tooltip(tooltipTriggerEl)
            });

            // Search functionality
            const searchInput = document.getElementById('searchInput');
            const statusFilter = document.getElementById('statusFilter');
            const typeFilter = document.getElementById('typeFilter');
            const accountsTable = document.getElementById('accountsTable');
            const rows = accountsTable.querySelectorAll('tbody tr');
            const selectAllCheckbox = document.getElementById('selectAll');
            const accountCheckboxes = document.querySelectorAll('.account-check');
            const selectedCount = document.getElementById('selectedCount');
            const bulkDeleteBtn = document.getElementById('bulkDeleteBtn');
            const resetFiltersBtn = document.getElementById('resetFilters');
            const exportBtn = document.getElementById('exportBtn');
            const bulkActionModal = new bootstrap.Modal(document.getElementById('bulkActionModal'));
            const selectedCountModal = document.getElementById('selectedCountModal');
            const confirmBulkDelete = document.getElementById('confirmBulkDelete');

            function filterTable() {
                const searchTerm = searchInput.value.toLowerCase();
                const statusValue = statusFilter.value;
                const typeValue = typeFilter.value;

                rows.forEach(row => {
                    const accountNumber = row.cells[1].textContent.toLowerCase();
                    const accountType = row.cells[2].textContent;
                    const status = row.cells[4].textContent;
                    
                    const matchesSearch = accountNumber.includes(searchTerm) || 
                                         accountType.toLowerCase().includes(searchTerm);
                    const matchesStatus = !statusValue || status.includes(statusValue);
                    const matchesType = !typeValue || accountType.includes(typeValue);

                    row.style.display = matchesSearch && matchesStatus && matchesType ? '' : 'none';
                });
            }

            // Event listeners for filtering
            searchInput.addEventListener('input', filterTable);
            statusFilter.addEventListener('change', filterTable);
            typeFilter.addEventListener('change', filterTable);

            // Reset filters
            resetFiltersBtn.addEventListener('click', function() {
                searchInput.value = '';
                statusFilter.value = '';
                typeFilter.value = '';
                filterTable();
            });

            // Bulk selection
            selectAllCheckbox.addEventListener('change', function() {
                accountCheckboxes.forEach(checkbox => {
                    checkbox.checked = selectAllCheckbox.checked;
                });
                updateSelectedCount();
            });

            accountCheckboxes.forEach(checkbox => {
                checkbox.addEventListener('change', function() {
                    updateSelectedCount();
                    selectAllCheckbox.checked = [...accountCheckboxes].every(cb => cb.checked);
                });
            });

            function updateSelectedCount() {
                const selected = [...accountCheckboxes].filter(cb => cb.checked).length;
                selectedCount.textContent = `${selected} account${selected !== 1 ? 's' : ''} selected`;
                selectedCountModal.textContent = selected;
                bulkDeleteBtn.disabled = selected === 0;
            }

            // Bulk delete
            bulkDeleteBtn.addEventListener('click', function() {
                if (!this.disabled) {
                    bulkActionModal.show();
                }
            });


            // confirmBulkDelete.addEventListener('click', function() {
            //     const selectedIds = [...accountCheckboxes]
            //         .filter(cb => cb.checked)
            //         .map(cb => cb.value);
                
            //     // Here you would typically make an AJAX call to delete multiple accounts
            //     console.log('Deleting accounts with IDs:', selectedIds);
                
            //     // For now, just reload the page
            //     bulkActionModal.hide();
            //     alert(`Deleting ${selectedIds.length} accounts... (This is a demo)`);
            // });


                    confirmBulkDelete.addEventListener('click', function () {
            const selectedIds = [...accountCheckboxes]
                .filter(cb => cb.checked)
                .map(cb => parseInt(cb.value));

            if (selectedIds.length === 0) return;

            fetch('/Account/BulkDelete', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                    'RequestVerificationToken':
                        document.querySelector('input[name="__RequestVerificationToken"]')?.value
                },
                body: JSON.stringify(selectedIds)
            })
            .then(response => {
                if (!response.ok) throw new Error('Delete failed');
                return response.json();
            })
            .then(data => {
                bulkActionModal.hide();
                location.reload(); // refresh table
            })
            .catch(error => {
                console.error(error);
                alert('Something went wrong while deleting accounts');
            });
        });



            // Export functionality
            exportBtn.addEventListener('click', function() {
                // Implement CSV export logic here
                alert('Export functionality would be implemented here');
            });

            // Initialize count
            updateSelectedCount();
        });
    </script> *@
}

<style>
    .table tbody tr:hover {
        background-color: rgba(0, 123, 255, 0.02);
        transition: background-color 0.2s ease;
    }
    
    .table tbody tr td {
        vertical-align: middle;
        padding: 1rem 0.75rem;
    }
    
    .card {
        border: none;
        border-radius: 10px;
    }
    
    .card-header {
        border-bottom: 1px solid rgba(0,0,0,.125);
        border-radius: 10px 10px 0 0 !important;
    }
    
    .btn-group .btn {
        border-radius: 6px !important;
        margin: 0 2px;
    }
    
    .badge {
        font-weight: 500;
        letter-spacing: 0.3px;
    }
    
    .form-control, .form-select {
        border-radius: 8px;
        border: 1px solid #dee2e6;
    }
    
    .input-group-text {
        border-radius: 8px 0 0 8px;
    }
    
    .form-check-input {
        width: 1.1em;
        height: 1.1em;
        cursor: pointer;
    }
    
    .form-check-input:checked {
        background-color: #0d6efd;
        border-color: #0d6efd;
    }
</style>